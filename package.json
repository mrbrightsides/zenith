{
  "name": "zenith-live",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:gcp": "vite build && firebase deploy --only hosting", 
    "deploy:prod": "vite build && vercel --prod",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "zenith-sync": "npm run deploy:prod && npm run deploy:rules"
  },
  "dependencies": {
    "@google/genai": "^1.42.0",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "firebase": "^12.9.0",
    "firebase-admin": "^13.6.1",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
